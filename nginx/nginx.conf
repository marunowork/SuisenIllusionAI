# SuisenGalleryAPI Lite - Nginx リバースプロキシ設定
#
# 設計書 30章(HTTPS) / 37章(タイムアウト設計) に基づく。
# 本番ではHTTPSを有効化し、HTTPからHTTPSへリダイレクトすること。
# 証明書取得後、443用serverブロックを追加し、下記のHTTPブロックを
# リダイレクト専用に変更する。

server {
    listen 80;
    server_name _;

    # 画像生成は時間がかかる可能性があるため、タイムアウトを長めに設定する
    proxy_connect_timeout 10s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    client_max_body_size 20m;

    location / {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- HTTPS有効化時の例(Let's Encrypt) ---
# server {
#     listen 443 ssl;
#     server_name example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     proxy_connect_timeout 10s;
#     proxy_send_timeout 300s;
#     proxy_read_timeout 300s;
#     client_max_body_size 20m;
#
#     location / {
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
